{
  "identifier": "default",
  "description": "Default desktop capability for the main window. Allows the local connect screen and any HTTPS Voice Hub instance the user enters.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://**", "http://localhost/**", "http://127.0.0.1/**"]
  },
  "permissions": [
    "core:default",
    "allow-shortcut",
    "allow-updater-app",
    "allow-connection",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
