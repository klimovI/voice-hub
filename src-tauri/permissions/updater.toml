"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-updater-app"
description = "Allow Voice Hub in-app updater commands (check/apply)."
commands.allow = [
    "check_for_update",
    "apply_update",
]
