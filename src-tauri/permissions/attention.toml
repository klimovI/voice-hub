"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-attention"
description = "Allow Voice Hub attention commands (flash_attention)."
commands.allow = [
    "flash_attention",
]
